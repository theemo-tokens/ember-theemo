// `config.js` is generated with actual config values in the build.

export interface TheemoConfig {
  options: {
    defaultTheme?: string;
    alternates: string[];
  };
  themes: {
    [key: string]: {
      colorSchemes: string[];
    };
  };
}

declare const config: TheemoConfig;

export default config;
